{
  "devDependencies": {
    "prettier": "3.9.6",
    "three": "0.160.0"
  },
  "name": "frost-defense",
  "version": "15.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "format": "prettier --write src scripts tests styles index.html README.md docs",
    "check": "npm run build && npm test"
  }
}
