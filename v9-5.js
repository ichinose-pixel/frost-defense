function makeRaider(scale=1){const g=new THREE.Group(),coat=scale>1?0x7a2c2c:0x454b58,body=box(.5,.58,.32,coat);body.position.y=1;g.add(body);addVoxelDetails(g,[[.46,.1,.35,0x242a34,0,.82,0],[.12,.12,.08,0x758096,-.16,1.12,.2],[.12,.12,.08,0x758096,.16,1.12,.2]]);const head=box(.36,.36,.36,0xd8b088);head.position.y=1.48;g.add(head);addVoxelDetails(g,[[.42,.13,.42,0x2d3440,0,1.68,0],[.08,.06,.03,0x15191f,-.1,1.51,.2],[.08,.06,.03,0x15191f,.1,1.51,.2]]);const legs=[];[[-.13,0],[.13,0]].forEach(([px,pz])=>{const l=box(.17,.54,.17,0x292f3a);l.geometry.translate(0,-.27,0);l.position.set(px,.54,pz);g.add(l);legs.push(l)});const arms=[];[[-.34,0],[.34,0]].forEach(([px,pz])=>{const a=box(.15,.54,.15,coat);a.geometry.translate(0,-.25,0);a.position.set(px,1.23,pz);g.add(a);arms.push(a)});g.scale.setScalar(scale);return{g,legs,arms}}
function colorizeEnemy(g,kind){const col=kind==='armored'?0x697989:kind==='breaker'?0x8a4d37:kind==='thrower'?0x6d597f:kind==='boss'?0x8f3030:null;if(!col)return;g.traverse(o=>{if(o.isMesh&&o.material){o.material=o.material.clone();o.material.color.lerp(new THREE.Color(col),.45)}})}
function chooseEnemyKind(){if(day===7&&waveLeft<=1)return'boss';const r=Math.random();if(nightModifier==='wolf'&&r<.78)return'wolf';if(nightModifier==='armored'&&r<.58)return'armored';if(nightModifier==='siege'&&r<.58)return'breaker';if(day>=4&&r<.16)return'thrower';if(day>=3&&r<.34)return'armored';if(day>=3&&r<.49)return'breaker';return r<.72?'wolf':'raider'}
function spawnEnemy(x=null,z=null,kindOverride=null){const a=Math.random()*Math.PI*2,r=R_INNER-1.2;if(x===null||z===null){x=Math.round(Math.cos(a)*r);z=Math.round(Math.sin(a)*r)}const kind=kindOverride||chooseEnemyKind(),scale=kind==='boss'?1.75:kind==='armored'?1.2:kind==='breaker'?1.15:1,model=kind==='wolf'?makeWolf():makeRaider(scale);colorizeEnemy(model.g,kind);const hpBase={wolf:26,raider:48,armored:105,breaker:78,thrower:54,boss:620}[kind]||48,hp=hpBase+day*(kind==='boss'?35:7);model.g.position.set(x,.5,z);const bb=box(kind==='boss'?1.25:.8,.1,.05,0x222222);bb.position.y=kind==='boss'?3.4:2.1;model.g.add(bb);const bf=box(kind==='boss'?1.25:.8,.12,.06,kind==='boss'?0xffa23a:0xff4444);bf.position.y=bb.position.y;model.g.add(bf);scene.add(model.g);const sp={wolf:3.45,raider:2.05,armored:1.25,breaker:1.7,thrower:1.6,boss:1.15}[kind],dmg={wolf:7,raider:11,armored:15,breaker:28,thrower:12,boss:38}[kind],ops=activeOutposts(),targetOutpost=(kind!=='boss'&&ops.length&&Math.random()<.42)?ops[Math.floor(Math.random()*ops.length)]:null;enemies.push({kind,model,hp,max:hp,sp,dmg,atkT:0,bar:bf,walkT:Math.random()*6,targetOutpost})}
function spawnEnemyPack(){if(waveLeft<=0)return;const a=Math.random()*Math.PI*2,r=R_INNER-1.2,baseX=Math.round(Math.cos(a)*r),baseZ=Math.round(Math.sin(a)*r),pack=(day===7&&waveLeft<=1)?1:Math.min(5,2+Math.floor(day/2)+(Math.random()<.6?1:0));for(let i=0;i<pack&&waveLeft>0;i++){const ox=(Math.random()*2-1)*1.5,oz=(Math.random()*2-1)*1.5;spawnEnemy(Math.round(baseX+ox),Math.round(baseZ+oz),null);waveLeft--}}
function shootArrow(from,dir,dmg,home){const m=box(.07,.07,.55,0xffe08a);m.position.copy(from);m.lookAt(from.clone().add(dir));scene.add(m);arrows.push({m,v:dir.clone().multiplyScalar(26),dmg,life:2,home:home||null})}
const keys={};addEventListener('keydown',e=>keys[e.code]=true);addEventListener('keyup',e=>keys[e.code]=false);let joyId=null,joyVec={x:0,y:0};const joyZone=$('joyZone'),joyBase=$('joyBase'),joyKnob=$('joyKnob');joyZone.addEventListener('pointerdown',e=>{if(!running)return;joyId=e.pointerId;joyZone.setPointerCapture?.(e.pointerId);joyBase.style.display=joyKnob.style.display='block';joyBase._ox=e.clientX;joyBase._oy=e.clientY;joyBase.style.left=(e.clientX-62)+'px';joyBase.style.top=(e.clientY-62)+'px';joyKnob.style.left=(e.clientX-27)+'px';joyKnob.style.top=(e.clientY-27)+'px'});addEventListener('pointermove',e=>{if(e.pointerId!==joyId)return;let dx=e.clientX-joyBase._ox,dy=e.clientY-joyBase._oy;const d=Math.hypot(dx,dy),max=48;if(d>max){dx*=max/d;dy*=max/d}joyKnob.style.left=(joyBase._ox+dx-27)+'px';joyKnob.style.top=(joyBase._oy+dy-27)+'px';joyVec={x:dx/max,y:-dy/max}});function endJoy(e){if(e.pointerId!==joyId)return;joyId=null;joyVec={x:0,y:0};joyBase.style.display=joyKnob.style.display='none'}addEventListener('pointerup',endJoy);addEventListener('pointercancel',endJoy);function bindCanvasLook(){renderer.domElement.addEventListener('contextmenu',e=>e.preventDefault())}function setMode(m){mode=m}function addFuel(){}
const ray=new THREE.Raycaster();function aimCell(){if(mode===1||mode===2||mode===3){const f=new THREE.Vector3(-Math.sin(yaw),0,-Math.cos(yaw)),p=pPos.clone().add(f.multiplyScalar(2.25)),x=Math.round(p.x),z=Math.round(p.z);if(mode===3){for(let d=1;d<=3;d++){const q=pPos.clone().add(new THREE.Vector3(-Math.sin(yaw),0,-Math.cos(yaw)).multiplyScalar(d)),qx=Math.round(q.x),qz=Math.round(q.z);for(let y=3;y>=1;y--){const b=blockAt(qx,y,qz);if(b&&(b.t==='wall'||b.t==='turret'))return{x:qx,y,z:qz,hit:b}}}}return{x,y:1,z,hit:blockAt(x,0,z)||null}}ray.setFromCamera({x:0,y:0},camera);const hits=ray.intersectObject(inst);for(const h of hits){if(h.instanceId===undefined||!blockArr[h.instanceId])continue;const b=blockArr[h.instanceId],n=h.face.normal;return{x:b.x+Math.round(n.x),y:b.y+Math.round(n.y),z:b.z+Math.round(n.z),hit:b}}return null}function doAction(){}
function addTurretVisual(x,y,z,level=1){const k=key(x,y,z);if(turretObjs.has(k))scene.remove(turretObjs.get(k));const g=new THREE.Group(),pole=box(.22,1.05,.22,level>=3?0x7b8ca2:0x586c80);pole.position.y=1.02;g.add(pole);addVoxelDetails(g,[[.6,.14,.6,level>=2?0x95aac0:0x7f94a8,0,1.55,0],[.74,.12,.18,0xaebed0,0,1.68,0],[.18,.12,.74,0xaebed0,0,1.68,0],[.14,.5,.14,0x3d2b1d,-.22,1.94,0],[.14,.5,.14,0x3d2b1d,.22,1.94,0],[.52,.08,.08,level>=2?0xffde89:0xe5bf76,0,2.05,0]]);if(level>=2)addVoxelDetails(g,[[.94,.08,.1,0xffd36b,0,2.18,0],[.1,.08,.94,0xffd36b,0,2.18,0],[.18,.18,.18,0xffd36b,0,2.35,0]]);if(level>=3)addVoxelDetails(g,[[.16,.8,.16,0x9ed7ff,0,2.52,0],[.34,.12,.34,0xffecad,0,2.96,0],[.12,.38,.12,0x8fe5ff,-.42,2.08,0],[.12,.38,.12,0x8fe5ff,.42,2.08,0]]);g.position.set(x,y,z);scene.add(g);g.userData.level=level;turretObjs.set(k,g)}
function solidAt(x,z){for(const[k,st]of defenseState){if(st.type!=='wall')continue;const[wx,wy,wz]=k.split(',').map(Number),tangent=Math.abs(wx)>=Math.abs(wz)?'z':'x',along=tangent==='x'?Math.abs(x-wx):Math.abs(z-wz),across=tangent==='x'?Math.abs(z-wz):Math.abs(x-wx);if(along<=1.52&&across<=.48)return blockAt(wx,wy,wz)}for(let y=1;y<=2;y++){const b=blockAt(Math.round(x),y,Math.round(z));if(b&&['wall','turret','flame','warehouse'].includes(b.t))return b}return null}
const PLAYER_RADIUS=.34;function playerCollidesAt(x,z){for(const[k,st]of defenseState){const[ox,oy,oz]=k.split(',').map(Number);if(st.type==='wall'){const tangent=Math.abs(ox)>=Math.abs(oz)?'z':'x',halfLong=1.52+PLAYER_RADIUS,halfShort=.48+PLAYER_RADIUS,along=tangent==='x'?Math.abs(x-ox):Math.abs(z-oz),across=tangent==='x'?Math.abs(z-oz):Math.abs(x-ox);if(along<halfLong&&across<halfShort)return true}else{const r=(st.type==='warehouse'?1.05:.72)+PLAYER_RADIUS;if(Math.hypot(x-ox,z-oz)<r)return true}}return false}
function nearestFreePosition(x,z){if(!playerCollidesAt(x,z))return{x,z};for(let radius=.35;radius<=3.5;radius+=.25){const steps=Math.max(12,Math.ceil(radius*18));for(let i=0;i<steps;i++){const a=i/steps*Math.PI*2,nx=x+Math.cos(a)*radius,nz=z+Math.sin(a)*radius;if(Math.max(Math.abs(nx),Math.abs(nz))>R_INNER-.45)continue;if(!playerCollidesAt(nx,nz))return{x:nx,z:nz}}}return{x:0,z:4}}
function movePlayerWithCollision(dx,dz){const lim=R_INNER-.45,sx=pPos.x,sz=pPos.z,tx=Math.max(-lim,Math.min(lim,sx+dx)),tz=Math.max(-lim,Math.min(lim,sz+dz));if(!playerCollidesAt(tx,tz)){pPos.x=tx;pPos.z=tz;return}if(!playerCollidesAt(tx,sz)){pPos.x=tx;return}if(!playerCollidesAt(sx,tz)){pPos.z=tz;return}if(playerCollidesAt(pPos.x,pPos.z)){const safe=nearestFreePosition(pPos.x,pPos.z);pPos.x=safe.x;pPos.z=safe.z}}